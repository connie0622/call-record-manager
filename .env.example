# .env example for local Docker Compose

# OpenAI API key is required for transcription and summarization
OPENAI_API_KEY=your_openai_api_key_here

# Optional: database URL; default uses local sqlite file
DATABASE_URL=sqlite:///./calls.db
